[build]
builder = "NIXPACKS"
watchPatterns = ["server/**"]

[build.nixpacksConfigPath]
path = "server/nixpacks.toml"

[deploy]
startCommand = "cd server && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
